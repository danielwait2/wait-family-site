# Wait Family Client

React + Vite single-page app for the Wait Family site.

## Setup

```bash
npm install
npm run dev
```

The dev server proxies API calls to `http://localhost:5001`.

## Environment

Create `client/.env` to override defaults. Example:

```bash
VITE_API_BASE_URL=http://localhost:5001
```
